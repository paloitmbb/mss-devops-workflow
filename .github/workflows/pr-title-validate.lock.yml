# AUTO-GENERATED — DO NOT EDIT MANUALLY
# Source: .github/workflows/pr-title-validate.md
# Regenerate with: gh aw compile pr-title-validate

name: PR Title Validator

on:
  workflow_call:

permissions:
  contents: read
  pull-requests: write

jobs:
  validate-pr-title:
    name: 🔍 Validate PR Title (Conventional Commits)
    runs-on: ubuntu-latest
    timeout-minutes: 5

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Run agentic workflow
        id: agent
        uses: github/agentic-runner@v1
        with:
          instructions_path: .github/workflows/pr-title-validate.md

      - name: Apply safe outputs
        uses: github/agentic-safe-output@v1
        with:
          outputs: ${{ steps.agent.outputs.safe-outputs }}
